# BE-04-02 — Implement `RenegotiationService` in Infrastructure

**Epic:** [BE-04 — Renegotiation Feature](../epics/BE-04-renegotiation-feature.md)  
**Status:** `[x]` Done

## Description
Implement `RenegotiationService` in `Infrastructure/` as the concrete implementation of `IRenegotiationService`.
All renegotiation logic, including persistence calls and domain model orchestration, lives here.

## Acceptance Criteria
- [x] `RenegotiationService` is placed in `Infrastructure/Services/`.
- [x] The class implements `IRenegotiationService` from Application.
- [x] All business renegotiation logic is fully contained in this class (no logic in the controller).
- [x] The service calls domain entity methods for invariant enforcement.
- [x] All dependencies (repositories, domain services) are injected via constructor.
- [x] All public methods have XML documentation.
- [x] All exception paths are handled and mapped to typed results — no raw exceptions escape.

## Technical Notes
- Follow the pattern: load aggregate → call entity method → persist → raise domain events.
- Do not inject `HttpContext` or any HTTP concern into this service.

## Definition of Done
`RenegotiationService` compiles, all renegotiation logic is self-contained, and no business rules exist in the controller.
