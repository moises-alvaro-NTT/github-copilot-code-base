# FE-06-03 — Document Design Tokens and Tailwind Conventions

**Epic:** [FE-06 — Documentation and Playground](../epics/FE-06-documentation-playground.md)  
**Status:** `[x]` Done

## Description
Create documentation pages in the playground that display all design tokens (colors, spacing, typography) and explain the Tailwind + `cn()` usage conventions.

## Acceptance Criteria
- [x] A "Design Tokens" story/page displays the full color palette with token names.
- [x] A "Typography" story/page shows all font sizes, weights, and line heights.
- [x] A "Spacing" story/page shows the spacing scale.
- [x] A "Utilities" story/page documents the `cn()` pattern with examples.
- [x] Token pages are generated from the `tokens.ts` source to stay in sync automatically.
- [x] Documentation is in English.

## Technical Notes
- Generate token pages from `Object.entries(tokens)` to avoid manual duplication.
- Include "do" and "don't" examples in the Utilities page.

## Definition of Done
Token and conventions pages render in Storybook/Ladle; no manual sync needed between code and docs.
