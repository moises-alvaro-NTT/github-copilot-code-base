# BE-04-06 — Integration Tests for Renegotiation Endpoint

**Epic:** [BE-04 — Renegotiation Feature](../epics/BE-04-renegotiation-feature.md)  
**Status:** `[x]` Done

## Description
Write integration tests for `POST /api/renegotiations` using `WebApplicationFactory<Program>` with an in-memory or test database.

## Acceptance Criteria
- [x] Test: `PostRenegotiations_ValidPayload_Returns200WithResultDto`
- [x] Test: `PostRenegotiations_MissingRequiredField_Returns400ProblemDetails`
- [x] Test: `PostRenegotiations_InvalidBusinessState_Returns422OrDomainError`
- [x] Tests use `WebApplicationFactory<Program>` or equivalent test host.
- [x] Response body is deserialized and asserted on DTO properties, not raw JSON strings.
- [x] Test names follow `Method_Condition_ExpectedResult` convention in English.
- [x] Database is seeded and cleaned between tests.

## Technical Notes
- Use `EF Core In-Memory` provider or `Testcontainers` for the test database.
- Assert response `Content-Type: application/json` on all responses.

## Definition of Done
Both integration test scenarios pass against the real HTTP stack; CI pipeline is green.
